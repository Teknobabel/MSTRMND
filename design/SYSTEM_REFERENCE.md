# Mastermind — system reference

This document describes the **current** architecture, data flow, and content model as of the repo state. Use it to stay consistent when adding features or content.

## Tech stack

- **Vite 6** + **TypeScript** (strict), ES modules.
- **Web UI**: DOM screens for menus (`index.html` + `src/navigation.ts`); **Canvas 2D** for the placeholder game view (`src/canvas/setup.ts`, `src/main.ts`).
- **Content**: JSON files under `content/`, validated with **Zod** in `src/game/contentSchema.ts` (issue-collecting pipeline; see **Content catalog pipeline**).
- **Validation CLI**: `npm run content:validate` runs `scripts/validate-content.ts` (Node + `tsx`; reports **every** issue in one pass). **`npm run typecheck`** runs `tsc --noEmit`. **`npm run build`** runs content validation, then typecheck, then `vite build`.
- **Tests**: **Vitest** (`npm test` / `npm run test:watch`); suites live next to sources as `src/game/*.test.ts` with shared fixtures in `src/game/testFixtures.ts` (`rawFixtureSlices`, `fixtureCatalog`, `seededRng`, `sequentialIds`).

## Repository layout (relevant paths)

| Path | Role |
|------|------|
| `content/*.json` | Designer-authored catalogs (traits, minions, **agents**, missions, locations, maps, assets, omega plans, **lairs**, **events**, organization names, **wanted levels**). |
| `src/game/types.ts` | Canonical TypeScript types for templates, catalog shape, and some runtime-only types. |
| `src/game/contentSchema.ts` | **`CONTENT_MANIFEST`** / **`CONTENT_SLICE_KEYS`** (slice key → `content/<key>.json`), exported per-entity Zod schemas + **`contentSliceSchemas`** (whole-file array schemas), **`ContentIssue`** collection (**`parseContentSlices`** shape phase, **`collectContentIssues`** semantic/cross-ref phase), **`parseContentCatalog(raw)`** → `{ catalog \| null, issues }`, and throwing **`parseCatalog(raw)`** (raises **`ContentValidationError`** listing every issue). `raw` is a **keyed** **`RawContentSlices`** record, not positional args. |
| `src/game/loadContent.ts` | Static-imports all JSON modules into a **`RawContentSlices`** record and returns **`parseCatalog(raw)`**. |
| `src/game/contentRefs.ts` | Content reference graph for tooling: **`collectContentReferences(catalog)`** (every id-reference with source path), **`referencesTo`** (delete-impact: who uses this entity), **`unreferencedIds`** (dead-content candidates). |
| `src/game/contentSerialize.ts` | **`serializeContentSlice(data)`** — canonical JSON for `content/*.json` writers (stable key order: `id`, `kind`, `name`, `type`, `description`, `cardArt`, then alphabetical; 2-space indent; trailing newline; array order preserved). Any content tool must write files through this. |
| `src/game/contentRename.ts` | **`applyIdRenameRaw(raw, slice, oldId, newId)`** — renames an entity id plus every referent across RAW slices (mirrors `contentRefs.ts` walkers; returns a new copy). **`RENAMABLE_SLICES`** = slices whose rows carry an `id`. |
| `editor.html` + `src/editor/**` | **Dev-only content editor** (see **Content editor** section). Not part of production builds (Vite only bundles `index.html`). |
| `src/game/testFixtures.ts` | Vitest fixtures: minimal valid raw slices, parsed fixture catalog, seeded RNG, sequential id generator. |
| `src/game/agent.ts` | **`createAgentFromTemplate`**, **`getAgentTemplateById`**, **`spawnOpposingAgentsAfterWantedEscalation`**, **`getOpposingAgentsAtLocation`**, **`getRevealedOpposingAgentsAtLocation`**, **`countOpposingAgentsAtLocation`**, **`countOpposingAgentsAtLocationFromData`**, **`revealAllOpposingAgentsAtLocation`**, **`totalOpposingAgentsAcrossLocations`** — agents extend minion instance shape with **`catalogVisibility`** (`hidden` \| `revealed`, default hidden); spawns on wanted tier increase in **`executePlan`**. **`spawnOpposingAgentsAfterWantedEscalation`** takes an optional **`newInstanceId`** generator (default `crypto.randomUUID`) for deterministic runs and only spawns into locations that have a **`locationAgentPresence`** row (no orphan instances). |
| `src/game/minion.ts` | Runtime: create minion from template (**`dynamicTraits`** from optional **`startingDynamicTraits`**); **mission XP → level-up** (`awardMissionResolutionExperience`, constants `MINION_XP_PER_MISSION` / `MINION_XP_TO_LEVEL`); ordered trait grant on level (`applyLevelUp` / `nextLevelUpTraitId`); add/remove trait; **hire-pool level gate** (**`templateStartingLevel`**, **`maxHireableStartingLevel`**, **`nextHireLevelInfamyThreshold`**). |
| `src/game/mission.ts` | Runtime: participant trait union, **`MissionSuccessOptions`** (extra traits + **`playerAssets`** or **`assignedAssetIds`** (per-slot plan / active mission) + **`dynamicTraitDelta`** + optional **`eventSuccessModifierDelta`** (timed global success % modifier from **`GameState.activeSuccessModifiers`**) + optional **`opposingAgentPenaltyCount`**), linear **success %** from matched **traits** + matched **asset slots** vs total slots, status trait deltas, **dynamic** flat delta, **event** flat delta, then flat **−20%** per opposing agent in that count; **`mergedRequiredTraitIdsSorted`**, **`countMultiset`**, **`matchedAssetUnits`**, **`countMatchedAssignedSlots`**, **`OPPOSING_AGENT_SUCCESS_PENALTY`**; roster size 1–**`player.maxParticipantsPerMission`** (default 3) via **`canAssignParticipants`** — **event** missions use a fixed **1–3** cap in **`assignMission`** / **`executePlan`** instead of **`maxParticipantsPerMission`**. |
| `src/game/dynamicTrait.ts` | **`materializePendingDynamicTraits`**, **`dynamicTraitSuccessModifierFromFullRoster`**, **`rollDynamicTraitsAfterMission`**, **`dynamicTraitDisplayLabel`**, **`formatStartingDynamicTraitsPreview`**, **`isPositiveDynamicTraitKind`** — Friend/Lover/Rival/Hatred/Hero/Wanted rules and **10%** post-mission rolls. |
| `src/game/missionEffects.ts` | **`orderedMissionEffects`**, **`applyMissionEffects`** (needs resolve-phase **`rng`** for random participant picks; returns updated **`player`** — including roster trait changes — plus **`locationSecurityStates`**, **`locationIntelStates`**, **`locationAssetSlots`**, and **`activeSuccessModifiers`** when effects touch those), **`describeMissionEffect`** / **`describeMissionTemplateEffects`**: mission completion effects after baseline infamy/heat in **`executePlan`** (reveal/steal/**`security_level_delta`**/**`security_level_delta_global`**/**`security_level_delta_by_location_type`**/**`security_level_delta_by_location_level`**/**`intel_level_delta`**/**`intel_level_delta_global`**/**`intel_level_delta_by_location_type`**/**`intel_level_delta_by_location_level`**/**`gain_assets`**/**`exchange_assets`**/**`add_target_minion_traits`**/**`add_random_participant_traits`**/**`add_all_participant_traits`**/**`remove_trait_from_all_minions`**/**`add_trait_to_random_minions`**/**`reveal_hidden_assets_global`**/**`reveal_hidden_assets_by_location_type`**/**`reveal_hidden_assets_by_location_level`**/**`grant_command_points_next_turn`**/**`add_success_chance_modifier`**, unlock lair mission template ids at resolve time, infamy, heat, stat caps). **`unlock_lair_mission`** is described here but **applied** in **`executePlan`** (updates **`lairMissionIds`**). |
| `src/game/lair.ts` | **`getLairById`**, **`pickRandomLairId`**, **`pendingLairUpgradeMissionIds`** (upgrade missions not yet completed successfully this run). |
| `src/game/locationCatalog.ts` | **getLocationById**, **getMapById**, **`locationTemplatesForOmegaPlan`**, **`activeLocationIds`**, **`initialLocationSecurityStatesForLocations`**, **`initialLocationAgentPresenceForLocations`**, **`maxSecurityLevelForLocation`**, **`rollLocationRequiredTraits`**, **`rollLocationSecurityTraits`**, **`rollInitialLocationIntelStates`** (run-start intel seeding). |
| `src/game/intel.ts` | **Site intel** — what the player is allowed to see at a location. **`MAX_INTEL_LEVEL`** (3) and the per-step thresholds **`INTEL_ASSET_EXISTENCE`** (1) / **`INTEL_ASSET_CONTENTS`** (2) / **`INTEL_AGENTS`** (3), **`clampIntelLevel`**, **`intelLevelForLocation`** / **`intelLevelAtLocation`**, **`assetSlotKnowledge`** → **`AssetSlotKnowledge`** (`unknown` \| `empty` \| `existence` \| `identified`) / **`isAssetSlotKnownToPlayer`**, **`effectiveAssetSlotVisibility`** / **`effectiveVisibilityOfSlot`**, **`isOpposingAgentVisibleToPlayer`**, **`playerVisibleOpposingAgentsAtLocation`** (+ `…FromData`), **`countPlayerVisibleOpposingAgentsAtLocation`**, **`totalPlayerVisibleOpposingAgents`**, and the pure appliers **`applyIntelLevelDelta`** / **`…Global`** / **`…ByLocationType`** / **`…ByLocationLevel`** used by `missionEffects.ts`. Intel is a **view over stored state**, never a rewrite of it (see **Site intel**). |
| `src/game/wantedLevel.ts` | **`tierIndexForHeat`**, **`nextMonotonicWantedTierIndex`**, **`wantedTierAtIndex`**, **`maxOpposingAgentsForWantedIndex`** — wanted tier from heat; monotonic index at end of **`executePlan`**. |
| `src/game/omegaPlan.ts` | Lookups: omega plan by id, **`pickRandomOmegaPlanId`** for new-run selection, mission id at stage/slot indices; phase-completion rules — **`omegaStageRequiredMissions`**, **`omegaPlanRequiredMissionTotal`**, **`isOmegaStageComplete`**. |
| `src/game/gameState.ts` | Runtime turn loop: **Execute Plan** runs **`executePlan`** (resolve missions, **event offer expire** when the offer was not engaged that Main Phase, **decay** **`activeSuccessModifiers`**, **rotate** **`currentEventTemplateId`** + **`event_rotated_in`** / **`event_expired`** activity), then **`advanceToNextTurn`** in the UI so the player returns to **Main** with CP refilled on the next tick — no separate **Next Turn** button. CP/infamy/heat/minions/assets, hire (**`pickHireOfferTemplateIds`** — infamy-gated offer draw), mission assignment, resolve rolls vs **`mission.ts`** (**`dynamicTraitDelta`** + **`eventSuccessModifierDelta`** + **`opposingAgentPenaltyCount`** at target sites); **`missionSuccessOptionsForTarget`**, **`revealedSecurityTraitIds`**, **`setLocationSecurityLevel`**, **`setLocationIntelLevel`**; **asset**-target validation in **`assignMission`** goes through the site's **effective** (intel-aware) visibility, not the stored slot flag; on each finished mission: **baseline + effect infamy and heat**, **`applyMissionEffects`** (results synced into the in-loop participant lookup so roster trait changes flow into the dynamic-trait roll, XP pass, and final merge), **`rollDynamicTraitsAfterMission`** (before critical injury / XP), **lair unlock effects** + **completed upgrade tracking**, **participant XP/level-ups**, **+1 security** at resolved target (**cap = that location’s `locationLevel`**) via **`getMissionTargetLocationId`**, **reveal all opposing agents** at that site when applicable, **`locationIntelStates`** carried through the resolve alongside **`locationSecurityStates`**; at end of **`executePlan`**, **monotonic `wantedLevelTierIndex`** from final **`player.heat`** vs **`catalog.wantedLevels`**, then if the tier **index increased**, **`spawnOpposingAgentsAfterWantedEscalation`** in **`agent.ts`** fills opposing agents up to the new tier’s **`maxAgents`** (random unused template, random playable map location; at most one instance per agent template id); **`activityLog`**; **`organizationName`** at run start. |
| `src/navigation.ts` | Menu state machine (main / settings / game / pause overlay). |
| `src/main.ts` | Boot: `loadContent()`, game controller (`initGameController`), canvas loop, `initNavigation()`; every UI action goes through one **`dispatch(action, { onError?, onApplied? })`** seam (runs the `Result`-returning transition, swaps `state`, calls `refresh()`; failures logged + surfaced via `onError`) — new handlers must use it rather than owning the state swap; mission success **previews** use **`countPlayerVisibleOpposingAgentsAtLocation`** (`intel.ts`) for **`opposingAgentPenaltyCount`**, **`dynamicTraitSuccessModifierFromFullRoster`** for **`dynamicTraitDelta`**, and the sum of **`GameState.activeSuccessModifiers`** for **`eventSuccessModifierDelta`**. |

## Content catalog pipeline

1. **Authoring**: Edit JSON under `content/`. Use stable string **`id`** fields for cross-references (lowercase slugs are typical).
2. **Loading (app)**: `loadContent()` in `loadContent.ts` static-imports every JSON file into a keyed **`RawContentSlices`** record (`{ traits, minions, agents, missions, locations, maps, assets, omegaPlans, lairs, events, organizationNames, playerProfiles, wantedLevels, balance }`) and calls **`parseCatalog(raw)`**. The slice list is defined once in **`CONTENT_MANIFEST`** (slice key → `content/<key>.json`); `scripts/validate-content.ts` iterates the same manifest. **`balance`** is a **single object**, not an array (see **Game balance**).
3. **Two validation phases** inside **`parseContentCatalog`** / **`parseCatalog`**:
   - **Shape phase** (**`parseContentSlices`**): each slice parsed **independently** against **`contentSliceSchemas[key]`** and normalized (e.g. `startingLevel` defaults to 1). A slice that fails shape is `null` in the result; its Zod errors become **`ContentIssue`** rows (`{ slice, entityId, path, message }`).
   - **Semantic phase** (**`collectContentIssues`**): array-level rules (duplicate ids, wanted-level ordering) plus the cross-slice reference checks — same logical dependencies as before: **traits** (defines `traitIds`) → **minions** (trait refs) → **agents** (same shape + trait refs; ids **disjoint** from minion template ids) → **assets** (defines `assetIds`) → **missions** (trait + asset refs + mission-effect placement rules) → **locations** → **`startingDynamicTraits` cross-check** on minion + agent templates → **maps** (location refs) → **omega plans** (mission refs + **`mapId`**) → **lairs** (mission refs + optional `startingAssets` asset refs; **`upgradeMissionIds`** disjoint from **`availableMissionIds`**) → **events** (ids **disjoint** from mission template ids; optional **`expireEffects`**; **both** requirement lists may be empty) → **wanted levels** (ordered heat tiers) → **balance** (`assetsPerLocationMin` ≤ `Max`; **`hireLevelInfamyThresholds`** strictly ascending; any minion **`startingLevel`** above `1 + thresholds.length` reported against the **minions** slice as unreachable by the hire pool). Checks whose inputs failed shape parsing are skipped; **all issues are collected**, never throw-on-first.
   - **`parseContentCatalog(raw)`** returns `{ catalog: ContentCatalog | null, issues: ContentIssue[] }` (`catalog` is null whenever issues exist). **`parseCatalog(raw)`** — used at boot and by the build gate — throws **`ContentValidationError`** whose message lists **every** issue. Content tooling should prefer the non-throwing form, the exported per-entity schemas (validate a single entity as it is edited), and **`collectContentIssues`** on a full draft catalog.

**Cross-reference rules** (enforced in `contentSchema.ts`):

- Minion `startingTraitIds` / `levelUpTraitOrder` → existing trait ids.
- Optional minion / agent **`startingDynamicTraits`**: each entry’s **`targetMinionTemplateId`** must reference an existing **minion** template id (not self); **`locationId`** must reference an existing location; no duplicate **`kind`+target** key; at most one positive bond (friend/lover) and one negative (rival/hatred) toward the same minion template; no **hero** and **wanted** for the same location on the same template.
- Agent templates (`content/agents.json`): same trait-reference rules as minions; **`id`** must not collide with any **minion** template **`id`**.
- Mission **`requiredTraitIds`** → traits; **unique** per mission. Mission **`requiredAssetIds`** → assets; **duplicates allowed** (each occurrence needs one inventory unit). **`gain_assets`** **`assetIds`** and **`exchange_assets`** **`removeAssetIds`** / **`gainAssetIds`** → assets; **duplicates allowed** (multiset). **`exchange_assets`** must have at least one id across the two lists. **At least one** of `requiredTraitIds` or `requiredAssetIds` must be non-empty per mission. Missions with **`reveal_target_asset`** or **`steal_target_asset`** in **`onSuccessEffects`** / **`onFailureEffects`** must use **`targetType`** `asset_hidden` or `asset_revealed`. **`reveal_all_hidden_assets_at_location`**, **`steal_all_assets_at_location`**, **`steal_all_revealed_assets_at_location`**, **`security_level_delta`**, and **`intel_level_delta`** require **`targetType`** `location`, `asset_hidden`, or `asset_revealed`. **`add_target_minion_traits`** requires **`targetType`** `minion`; **`traitIds`** must reference existing traits and be **unique** within the effect (≥ 1 entry). **`add_random_participant_traits`** / **`add_all_participant_traits`** **`traitIds`** → existing traits, **unique** within the effect (≥ 1 entry). **`unlock_lair_mission`** may appear only in **`onSuccessEffects`** (not **`onFailureEffects`**); **`missionId`** must reference an existing mission template.
- Omega plan **`mapId`** → **`MapTemplate.id`**; omega plans parsed **after** maps.
- Omega plan stage `missionIds` (3×3 grid) → missions; **same mission id may repeat** within a plan. Stage **`requiredMissions`** is shape-validated to **1–3** (absent ⇒ **3**).
- **Lair** **`availableMissionIds`** / **`upgradeMissionIds`** → missions; no duplicate mission id **within** each list; the two lists are **disjoint** per lair; **`startingAssets`** keys → asset ids.
- **Events** (`content/events.json`): **`EventTemplate`** extends **`MissionTemplate`** with optional **`expireEffects`** (same **`MissionEffect`** union as success/failure lists; **`unlock_lair_mission`** forbidden in **`expireEffects`**). **`id`** must not collide with any **mission** template **`id`**. **`requiredTraitIds`** / **`requiredAssetIds`** are validated like missions, but **both may be empty** (unlike missions, which require at least one non-empty requirement list). Effect placement rules match missions for target-dependent kinds.
- Map `locationIds` → locations; no duplicate location id **within the same map**; a location may appear on multiple maps.
- Asset ids unique in `assets.json`.
- **`organizationNames.json`**: JSON array of at least one non-empty string (no cross-references).
- **`wantedLevels`** (`content/wantedLevels.json`): ordered array of tiers (**`minHeat`** 0–100 strictly ascending, first tier **`minHeat === 0`**; tiers key off **heat**, not infamy), **`name`** (display), **`maxAgents`** (non-decreasing integers ≥ 0 — cap on opposing agents in play for that tier). Ordering rules enforced in **`collectContentIssues`**.

## Domain objects (catalog templates)

Enums in JSON are generally **lowercase** strings (e.g. trait `type`, location `locationType`).

### Traits (`content/traits.json`)

- **Trait**: `id`, `name`, `type`: `status_positive` | `status_negative` | `primary` | `secondary`.
- Passive definitions; minions, agents, and missions reference trait ids. **`status_positive`** / **`status_negative`** are excluded from per-location site/security rolls (see **Locations**). On missions, each participating minion’s trait list contributes flat success modifiers: **+10%** per **`status_positive`** occurrence and **−20%** per **`status_negative`** occurrence (summed across all participants), applied after the linear base % and clamped with the final result to **0–100**.
- **Dynamic traits** (runtime only, not catalog `Trait` rows; stored on **`MinionInstance.dynamicTraits`**; see **`dynamicTrait.ts`**): kinds **`friend`** / **`lover`** / **`rival`** / **`hatred`** (toward another roster minion **`instanceId`**) and **`hero`** / **`wanted`** (toward a **`locationId`**). Flat success modifiers (summed across participants for the mission roll and UI preview): Friend **+5%**, Lover **+10%**, Rival **−5%**, Hatred **−10%**, Hero **+5%**, Wanted **−5%**, each applying when the linked minion is on the mission or the mission’s **`getMissionTargetLocationId`** matches the location trait. After **`applyMissionEffects`** on resolve, each participant independently rolls **10%** for Friend (success, **>1** participants) or Rival (failure, **>1** participants), and for Hero (success) / Wanted (failure) when the mission has a location id; Friend→Lover and Rival→Hatred upgrade when the roll targets an existing base bond; opposite-side bonds **replace** the prior bond for that target; Hero/Wanted replace each other per location. Designer optional **`startingDynamicTraits`** on **`MinionTemplate`** (see **Minions**) is validated in **`contentSchema.ts`** after locations load.

### Minions (`content/minions.json`)

- **MinionTemplate**: `id`, `name`, `description`, **`hireCommandPoints`** (integer ≥ 0; CP spent when hiring in the Main Phase), optional `startingTraitIds`, optional **`startingLevel`** (integer ≥ 1, default **1**; grants sequential traits from **`levelUpTraitOrder`** via the same rules as automatic level-ups until **`currentLevel`** reaches this value — **and** gates when the template may appear in the hire pool, see **Hire-pool infamy gate** under **Runtime game state**), `levelUpTraitOrder` (ordered trait ids for further level-ups), optional **`startingDynamicTraits`**: designer-authored runtime bonds (`friend` \| `lover` \| `rival` \| `hatred` + **`targetMinionTemplateId`**, or `hero` \| `wanted` + **`locationId`**; validated for refs, self-target, duplicates, and hero/wanted or positive/negative conflicts per **`contentSchema.ts`**).
- **MinionInstance** (runtime, not in JSON): **`instanceId`** (stable id for roster and missions), `templateId`, `currentLevel`, `currentExperience`, `traitIds`, **`dynamicTraits`** (array of **`DynamicTrait`** — relationship/location modifiers separate from catalog **`traitIds`**).
- **Creation** (`minion.ts`): `createMinionFromTemplate(template, instanceId, overrides?)` — callers supply `instanceId` (e.g. `crypto.randomUUID()`).
- **Mission XP & leveling** (`minion.ts` + **`executePlan`** in `gameState.ts`): when a mission **resolves** (duration reaches 0 with valid template and participants), **each participant** gains **`MINION_XP_PER_MISSION`** (**1**) XP. When **`currentExperience`** reaches **`MINION_XP_TO_LEVEL`** (**3**), the minion **levels up** (`currentLevel` +1), **`currentExperience` resets to 0**, and **`applyLevelUp`** runs: grant the **first** trait in `levelUpTraitOrder` the instance does **not** already have (if none left, level still increases but **no** new trait). A **`minion_leveled_up`** activity event is recorded for that turn (see **Activity log** below).
- **Manual / catalog level-up helpers** (`minion.ts`): `nextLevelUpTraitId`, `applyLevelUp`, `addTrait`, `removeTrait` — same ordered trait rule as automatic level-ups.
- **Hire availability** (`minion.ts`): **`templateStartingLevel`** (`startingLevel ?? 1`, floored at 1), **`maxHireableStartingLevel(infamy, thresholds)`**, and **`nextHireLevelInfamyThreshold`** (the UI's "next tier at N infamy" line). A template only enters the hire draw once **`player.infamy`** has unlocked its **`startingLevel`** — see **Hire-pool infamy gate** under **Runtime game state**.

### Agents (`content/agents.json`)

- **AgentTemplate**: Same fields as **MinionTemplate** (`types.ts` aliases `AgentTemplate` to `MinionTemplate`). **`hireCommandPoints`** is validated like minions (often **`0`** for non-hirable opposition). Trait rules match minions.
- **`AgentInstance`**: **`MinionInstance`** plus **`catalogVisibility`**: `hidden` \| `revealed` (default **`hidden`** for spawns). Not stored on **`PlayerState`**. Instances live on **`GameState.opposingAgentInstances`**; which location each belongs to is tracked in **`GameState.locationAgentPresence`** (see **Locations**). Location cards list **all** agents at the site for debug: **revealed** names plain, **hidden** names in parentheses.
- **Creation** (`agent.ts`): **`createAgentFromTemplate`** delegates to **`createMinionFromTemplate`** so leveling helpers in **`minion.ts`** apply unchanged when you spawn runtime agents later.
- **Lookup / spawn** (`agent.ts`): **`getAgentTemplateById`**, **`getOpposingAgentByInstanceId`**, **`getOpposingAgentsAtLocation`**, **`getRevealedOpposingAgentsAtLocation`** (subset helper), **`countOpposingAgentsAtLocation`** / **`countOpposingAgentsAtLocationFromData`** (success % helpers), **`revealAllOpposingAgentsAtLocation`** (runtime reveal), **`spawnOpposingAgentsAfterWantedEscalation`** (wanted tier index must **increase** that resolve; fills toward **`maxOpposingAgentsForWantedIndex`** with random unused **`catalog.agents`** template and random playable **`locationId`** from the active omega map; new instances default to **`catalogVisibility: "hidden"`**).

### Missions (`content/missions.json`)

- **MissionTemplate**: `id`, `name`, `description`, **`targetType`**: `location` | `asset_hidden` | `asset_revealed` | `minion` | `none`, **`startCommandPoints`** (≥ 0), **`requiredTraitIds`** (unique trait ids; may be empty if assets carry the requirement), **`requiredAssetIds`** (catalog asset ids; **multiset** — duplicate ids mean multiple slots; each filled slot commits one matching unit from **`PlayerState.assets`** at **`assignMission`** and is stored on **`ActiveMission.plannedAssetIds`**), **`durationTurns`** (≥ 1), optional **`onSuccessEffects`** / **`onFailureEffects`**: arrays of **`MissionEffect`** (applied in order when a mission resolves; see **`missionEffects.ts`**). **`MissionEffect`** `kind` values: `reveal_target_asset`, `reveal_all_hidden_assets_at_location`, `steal_target_asset`, `steal_all_assets_at_location`, `steal_all_revealed_assets_at_location`, `unlock_lair_mission` (`missionId`), **`gain_assets`** (`assetIds`: multiset of catalog asset ids — duplicates grant multiple units), **`exchange_assets`** (`removeAssetIds`, `gainAssetIds`: multisets — removes up to inventory holdings per id, then grants gains), **`security_level_delta`** (`delta`: signed change to **`securityLevel`** at the mission location — negative reduces — clamped to **`[0, locationLevel]`**), **`security_level_delta_global`** (signed delta applied to **every playable location**, each clamped to **`[0, that location’s locationLevel]`**), **`security_level_delta_by_location_type`** / **`security_level_delta_by_location_level`** (same clamp semantics, filtered by **`locationType`** or **`locationLevel`**), **`intel_level_delta`** (`delta`: signed change to **`intelLevel`** at the mission location — negative reduces — clamped to **`[0, 3]`**; the surveillance lever, see **Site intel**), **`intel_level_delta_global`** / **`intel_level_delta_by_location_type`** / **`intel_level_delta_by_location_level`** (same clamp, scoped like their security twins — how **events** raise or lower intel without a target), **`reveal_hidden_assets_global`** / **`reveal_hidden_assets_by_location_type`** / **`reveal_hidden_assets_by_location_level`** (reveal up to **`count`** random **hidden occupied** asset slots in scope), **`remove_trait_from_all_minions`** (**`traitId`**), **`add_trait_to_random_minions`** (**`traitId`**, **`count`** — pool = full hired roster **including busy**), **`grant_command_points_next_turn`** (**`amount`** → **`pendingBonusCommandPoints`**), **`add_success_chance_modifier`** (**`delta`**, **`turns`** → **`activeSuccessModifiers`**), **`add_target_minion_traits`** (`traitIds`: unique catalog trait ids; grants any not-already-held to the **target minion** in **`player.minions`**; mission **`targetType`** must be `minion`), **`add_random_participant_traits`** (`traitIds`: unique catalog trait ids; grants any not-already-held to one participant chosen uniformly from **`participantInstanceIds`** using the resolve-phase RNG), **`add_all_participant_traits`** (same **`traitIds`** rules; grants any not-already-held to **every** participant in **`participantInstanceIds`**), `infamy_delta` (`amount`), `heat_delta` (`amount`), `max_concurrent_missions_delta`, `max_roster_size_delta`, `max_hire_offers_delta`, `max_participants_per_mission_delta`, `max_command_points_per_turn_delta` (each with signed integer **`delta`** except infamy). Baseline infamy (+5 success / 0 failure) and baseline heat (0 success / +5 failure) apply first, then effect deltas; **`infamy_delta`** / **`heat_delta`** effects **add**; both are clamped 0–100 at the end. **`orderedMissionEffects`** runs all **reveal** kinds first, then steal kinds (`steal_target_asset`, `steal_all_assets_at_location`, `steal_all_revealed_assets_at_location`), then the rest (including **`unlock_lair_mission`**, **`gain_assets`**, **`exchange_assets`**, **`security_level_delta`**, **`security_level_delta_global`**, scoped security deltas, hidden-asset reveals, **`add_target_minion_traits`**, **`add_random_participant_traits`**, **`add_all_participant_traits`**, roster trait sweeps, **`grant_command_points_next_turn`**, **`add_success_chance_modifier`**); **`steal_all_assets_at_location`** reveals hidden assets at the mission location then empties every occupied slot into inventory; **`steal_all_revealed_assets_at_location`** empties only **revealed** occupied slots; **`gain_assets`** adds to **`player.assets`** without changing locations; **`exchange_assets`** applies **`asset_lost`** / **`asset_gained`** for inventory changes; other steals leave **`empty`** slot(s). **`add_target_minion_traits`** appends each missing trait id to that minion's **`traitIds`** (no duplicates; ordering preserved); **`add_random_participant_traits`** does the same for one random roster minion in **`participantInstanceIds`**; **`add_all_participant_traits`** does the same for **all** of them; **`executePlan`** then syncs its participant lookup with updated minions so XP / level-up runs on top of new traits. **`unlock_lair_mission`** appends **`missionId`** to **`GameState.lairMissionIds`** if not already present (handled in **`executePlan`** after **`applyMissionEffects`**).
- **Success chance** (`mission.ts` + callers): **`successChancePercent(template, participants, options?)`** where **`MissionSuccessOptions`** may include **`additionalRequiredTraitIds`** (from **`missionSuccessOptionsForTarget`** in `gameState.ts`: site **required** traits + **revealed** security traits for the mission’s target location), **`playerAssets`** (inventory snapshot for the asset portion **only when** **`assignedAssetIds`** is omitted or its length does not match **`template.requiredAssetIds`**), **`assignedAssetIds`** (parallel to **`requiredAssetIds`**: each index is **`null`** or the matching catalog id; when provided with matching length, counts toward the base % instead of **`playerAssets`**), **`traitsCatalog`** (the parsed **`catalog.traits`** slice — when present, status trait modifiers apply), **`dynamicTraitDelta`** (flat sum from **`dynamicTraitSuccessModifierFromFullRoster`** in **`dynamicTrait.ts`**, using materialized pending template bonds against the full roster), **`eventSuccessModifierDelta`** (flat sum of **`delta`** from **`GameState.activeSuccessModifiers`** — timed modifiers from **`add_success_chance_modifier`**; each entry’s **`turnsRemaining`** decrements once per **`executePlan`**), and **`opposingAgentPenaltyCount`** (non-negative integer; each unit applies **−20%** after status, dynamic, and event modifiers). **Base %**: numerator = matched **distinct** required traits (participant union) + matched **asset slots** (either **`countMatchedAssignedSlots`** when **`assignedAssetIds`** matches length, else per-id `min(requiredCount, quantity)` from **`matchedAssetUnits`**); denominator = trait count + **asset occurrence count**; `Math.round(100 * matched / total)`; empty denominator → base **100%**. **Final %**: base plus **+10** per participating **`status_positive`** trait id on roster minions minus **20** per **`status_negative`** occurrence (each minion’s `traitIds` entry counts once per id), plus **`dynamicTraitDelta`**, plus **`eventSuccessModifierDelta`**, minus **20** × **`opposingAgentPenaltyCount`**, then clamped to **0–100**. **Opposing agents** (see **Locations**): only when the mission target maps to a **site** via **`getMissionTargetLocationId`** (location- or asset-targeted missions). **`executePlan`** passes the **full** agent count at that site (hidden + revealed) so the roll and **`mission_completed.successChancePercent`** reflect reality. **Plan / active mission UI** (`main.ts`) passes only the agents **visible to the player** (revealed by play, or any agent at a site with intel **3** — **`countPlayerVisibleOpposingAgentsAtLocation`**) so opposition the player cannot see does not lower the **displayed** success %; the **Activity** line `roll … vs N%` shows the **true** resolve-time chance (including hidden agents and **dynamic** / **event** modifiers). **`minion`** / **`none`** targets omit the opposing-agent modifier (no site location). Participants: 1–**`maxParticipantsPerMission`** minions for **lair** / **omega**; **event** missions use **1–3** fixed (see **Events**).

### Events (`content/events.json`)

- **`EventTemplate`**: Same fields as **`MissionTemplate`**, plus optional **`expireEffects`**: **`MissionEffect[]` applied automatically at the end of `executePlan`** if the player **did not** assign the **`GameState.currentEventTemplateId`** offer during that turn’s Main Phase (**`eventOfferEngagedThisTurn`** stayed false). Expire uses a stub **`ActiveMission`** (no participants / no target) so participant-only random effects are no-ops. Ignoring an offer (no drag to Plan) is implicit — there is no Decline button.
- **Runtime offer**: **`GameState.currentEventTemplateId`** is one catalog **`EventTemplate.id`** (or **`null`** when **`catalog.events`** is empty), chosen at run start and **re-rolled uniformly (with replacement) at the end of every `executePlan`** after optional expire + modifier decay. **`event_rotated_in`** records the new id in the activity log.
- **Starting an event**: Assign with **`missionSource === "event"`**, **`missionTemplateId === currentEventTemplateId`**, **1–3** participants (ignores **`player.maxParticipantsPerMission`**), omega indices **`null`**. Counts toward **`player.maxConcurrentMissions`**. Same **`ActiveMission`** pipeline as lair/omega for resolve, success %, and **`applyMissionEffects`**.
- **Timed success modifiers**: Effects **`add_success_chance_modifier`** push **`{ delta, turnsRemaining }`** onto **`GameState.activeSuccessModifiers`**. Each **`executePlan`** adds the total **`delta`** into resolve rolls and UI previews via **`eventSuccessModifierDelta`**, then decrements **`turnsRemaining`** and drops expired entries.
- **Bonus CP next turn**: **`grant_command_points_next_turn`** adds to **`player.pendingBonusCommandPoints`**; **`advanceToNextTurn`** sets **`commandPoints = maxCommandPoints + pendingBonusCommandPoints`**, then clears the bonus field.
- **Global / scoped effects**: **`security_level_delta_global`**, **`security_level_delta_by_location_type`**, **`security_level_delta_by_location_level`**, and the **`reveal_hidden_assets_*`** variants iterate **playable** locations from the active omega map; security deltas clamp per site to **`[0, locationLevel]`**. **`remove_trait_from_all_minions`** strips a trait id from every hired roster minion. **`add_trait_to_random_minions`** picks up to **`count`** distinct hired minions uniformly at random (**including minions busy on missions**), grants the trait if missing.

### Mission targets (runtime + planning UI)

- **`MissionTarget`** (runtime union in `types.ts`, stored on **`ActiveMission.target`**): `location` (`locationId`), `asset` (`locationId`, `slotIndex`, `visibilityAtAssign`), `minion` (`instanceId`), or `none`. Asset targets reference a **`LocationAssetSlot`** row at run time (by index into **`GameState.locationAssetSlots`** for that location).
- **`assignMission`** validates that the chosen **`MissionTarget`** matches the mission template’s **`targetType`** (see **`missionTargetMatchesTemplate`**). **Location** and **asset** targets require a **playable** map location; **asset** also requires the slot to exist, to be **`occupied`** (not **`empty`**), and **`visibilityAtAssign`** to match the slot’s **effective** visibility — its stored visibility raised by site **intel** ≥ 2 (see **Site intel**). **Minion** targets must be a roster minion not on another mission and **not** among the participants. **`none`** skips target validation.
- **Security bump** on resolve: **`+1`** at **`getMissionTargetLocationId(am.target)`** when non-null, capped at **`maxSecurityLevelForLocation`** = that site’s authored **`locationLevel`** (1–3), not a global 3 for all sites. **Location**- and **asset**-targeted missions affect that asset’s **location**; **minion** / **none** do not change security by target (`raiseSecurityAfterMissionAtLocation` in **`gameState.ts`**). **`setLocationSecurityLevel(state, catalog, locationId, level)`** clamps security for future systems that lower heat; revealed security traits follow **`securityLevel`** (see **Runtime game state**).
- **Plan mission UI** (`main.ts`): **Mission** slot and **Target** slot can be filled in any order. The target **label** updates with the mission’s **`targetType`** (e.g. “Target Location”). If **`targetType === "none"`**, the whole target field (**`#assign-target-field`**) is **hidden**. Dropping a mission that **mismatches** the staged target **clears** the target slot (**`reconcileTargetWithMission`**). **Asset** rows on location cards are **draggable chips** (per-slot identity) carrying the slot's **effective** visibility, so which slots can be targeted — and as `asset_hidden` or `asset_revealed` — follows that site's intel. Roster minion drags use JSON payloads. Putting a minion in **participants** clears the same minion from the **target** slot, and vice versa. When the planned mission has **`requiredAssetIds`**, a **Required Assets** fieldset lists one drop slot per occurrence; **Assets** panel cards drag with **`mastermind-asset-card`** JSON (type-matched slot only; **Available** counts subtract units already staged in the plan).

### Locations (`content/locations.json`)

- **LocationTemplate**: `id`, `name`, `description`, `locationType` (`political` | `military` | `economic`), **`locationLevel`** (1 | 2 | 3). Per-location **asset placement** is **not** in this JSON; at run start **`createInitialGameState`** assigns **1–3** random catalog assets per **playable** location on the active map (`LocationAssetSlot`: **`kind`** `occupied` with **`assetId`** + **`visibility`** `hidden` | `revealed`, or **`kind`** `empty` after a steal). **Every slot starts `hidden`** — the run's opening visibility comes from seeded **intel** instead (see **Site intel**).
- **LocationSecurityState** (runtime): `locationId`, **`securityLevel`** (`0` | `1` | `2` | `3`). New runs start at **`0`** via **`initialLocationSecurityStatesForLocations`**. Effective maximum per site is the location’s **`locationLevel`** (enforced when raising security). **During play**, each resolved mission at that site (via **`getMissionTargetLocationId`**) increments security by **1** up to that cap.
- **LocationIntelState** (runtime): `locationId`, **`intelLevel`** (`0` | `1` | `2` | `3`). Seeded per run by **`rollInitialLocationIntelStates`** (a few sites open above 0; see **Site intel**); the cap is a fixed **3** for every site (**`MAX_INTEL_LEVEL`**, structural — each level unlocks one specific kind of knowledge, so it is *not* a `BalanceConfig` knob). Moved by the **`intel_level_delta*`** mission effects (surveillance missions raise it; events raise or lower it). See **Site intel**.
- **Per-run site traits** (runtime, `GameState`): **`locationRequiredTraits`**: rolled at run start from **primary + secondary** traits only (excludes **`status_positive`** / **`status_negative`**) — level **1** → 0 traits, level **2** → 1, level **3** → 2. **`locationSecurityTraits`**: rolled stack of length **`locationLevel`** (reveal order = array order); only the first **`securityLevel`** entries merge into mission requirements (hidden entries do not). Both merge into **`missionSuccessOptionsForTarget`** for **location**/**asset** targets (see **`revealedSecurityTraitIds`**).
- **Opposing agents at sites** (runtime, `GameState`): **`locationAgentPresence`** lists **`AgentInstance.instanceId`** values per playable **`locationId`** (starts empty). Full **`AgentInstance`** rows live in **`opposingAgentInstances`** (each has **`catalogVisibility`**; spawns default **hidden**). Not authored in **`locations.json`**; **`spawnOpposingAgentsAfterWantedEscalation`** appends new ids to both when wanted escalates. **Locations** cards show an **Agents** row listing only the agents the player may see (**`catalogVisibility === "revealed"`** *or* site intel **3** — see **Site intel**); agents the player has not uncovered are omitted entirely rather than hinted at. Each opposing agent at the mission’s **target site** (same location id as **`getMissionTargetLocationId`**) reduces success chance by **20%** at resolve — **all** of them, seen or not; when a mission with such a target **resolves**, every opposing agent at that site becomes **`revealed`** (success or failure).

### Site intel (`intel.ts`, runtime)

**`GameState.locationIntelStates`** holds one **`LocationIntelState`** per playable location (**`intelLevel`** `0`–`3`). Intel decides **what the player may see** at a site; it never changes the site itself, so the success roll, the opposing-agent penalty, and every other rule keep using the true state.

**Run start** (**`rollInitialLocationIntelStates`** in `locationCatalog.ts`): every asset slot on the map is generated **hidden**, and the player's opening leads come from intel alone — **`balance.initialIntelSitesAtOne`** (default **2**) random playable sites open at intel **1** and **`balance.initialIntelSitesAtTwo`** (default **1**) further sites open at intel **2**. Both tiers are drawn from the same pool **without replacement**, so the picks are distinct and no site can open above 2 (agents are never visible on turn 1). Every other site starts dark. This replaced the old **`initialRevealedAssetSlots`** pass, which flipped N random slots to `revealed` at run start.

| Intel | Unlocks |
|---|---|
| **0** | Nothing. Asset slots that are still `hidden` are **not listed at all** (the player cannot even count them); opposing agents are invisible. Anything uncovered **through other means** stays visible. |
| **1** | Every asset slot at the site is listed. Contents stay unknown — the row reads `Asset`. |
| **2** | Asset contents are identified, and the slots read as **revealed** for mission targeting too (see below). |
| **3** | Opposing agents at the site are visible, **including any that arrive later** while intel is still 3 — visibility is computed at read time, not stamped on the instance. |

- **Intel is a view, not a rewrite.** **`LocationAssetSlot.visibility`** and **`AgentInstance.catalogVisibility`** record what was uncovered *through other means* (reveal/steal effects, resolving a mission at the site) and are permanent; intel is layered on top via **`effectiveAssetSlotVisibility`** / **`isOpposingAgentVisibleToPlayer`**. That is what lets an event **lower** intel and actually cost the player information — knowledge granted only by intel goes away, knowledge earned by play does not.
- **Effective visibility is the visibility mission targeting uses.** At intel ≥ 2 a stored-`hidden` slot is a valid **`asset_revealed`** target (and no longer a valid **`asset_hidden`** one): **`assignMission`**, the plan-panel drop handler, the draggable asset chips, and **`steal_all_revealed_assets_at_location`** all go through `intel.ts` instead of reading `slot.visibility` directly. **`ActiveMission.visibilityAtAssign`** is fixed at assign time, so a later intel change never invalidates a mission already in flight.
- **UI reads player-visible counts**: the location card's **Intel level** row (tooltip lists the table above), the **Agents** row, the HUD **Agents** total (**`totalPlayerVisibleOpposingAgents`**), and the mission success **preview** (**`countPlayerVisibleOpposingAgentsAtLocation`**) — hidden opposition never moves the displayed chance, while the resolve roll still counts it.
- **Changing intel**: the **`intel_level_delta`** family of mission effects (see **Missions**), or **`setLocationIntelLevel(state, locationId, level)`** in `gameState.ts` for other systems. Both clamp to `[0, MAX_INTEL_LEVEL]`.

### Maps (`content/maps.json`)

- **MapTemplate**: `id`, `name`, `description`, `locationIds` (ordered list of location ids).

### Assets (`content/assets.json`)

- **Asset**: `id`, `name`, optional `description`. Referenced by **missions** (`requiredAssetIds`), **lairs** (`startingAssets`), and **player inventory** **`PlayerState.assets`** (`Record<assetId, quantity>`). **Mission success %** uses **`assignedAssetIds`** on active missions (reserved inventory) or **`playerAssets`** when no per-slot list applies; the **Assets** panel shows **Available** (owned minus units staged in the plan mission UI).

### Lairs (`content/lairs.json`)

- **LairTemplate**: `id`, `name`, optional `description`, **`availableMissionIds`** (missions shown under the Lair **Missions** tab and assignable with **`missionSource === "lair"`**; no duplicate ids per lair), **`upgradeMissionIds`** (optional in JSON, default **[]**; **disjoint** from **`availableMissionIds`** — one-time **upgrade** missions listed for the Lair **Upgrades** tab), optional **`startingAssets`** (`Record<assetId, quantity>` merged into **`PlayerState.assets`** at run start). **`GameState.activeLairId`** is chosen at random when **`catalog.lairs`** is non-empty; **`lairMissionIds`** starts as a copy of **`availableMissionIds`** and grows when **`unlock_lair_mission`** succeeds or via **`addLairMissionToPool`**. **`completedLairUpgradeMissionIds`** tracks upgrade missions that have **resolved successfully** once this run (they disappear from the Upgrades tab).

### Omega plans (`content/omegaPlans.json`)

- **OmegaPlanTemplate**: `id`, `name`, `description`, optional **`cardArt`** (rendered in the Omega Plan panel header **only when authored** — no default placeholder), **`mapId`** (must match **`MapTemplate.id`**), `stages` — exactly **3** stages, each with **`missionIds` of length 3** (fixed 3×3 grid) and **`requiredMissions`** (designer-authored **1–3**; **omitted in JSON means 3**, the legacy all-slots rule) — how many of that phase's missions must succeed before the phase clears. Win-path content only; **grid layout** is in JSON. Helpers in **`omegaPlan.ts`**: **`omegaStageRequiredMissions`** (clamped lookup), **`omegaPlanRequiredMissionTotal`**, **`isOmegaStageComplete`**. **Runtime progress** lives in **`GameState.omegaStageProgress`** (per-slot success flags for **all three** phases, `[stage][slot]`, never reset) plus **`activeOmegaStageIndex`**; **`executePlan`** marks each successful omega slot in **its own** phase row and advances the stage once the current row reaches **`requiredMissions`** (see **`gameState.ts`**).

### Organization names (`content/organizationNames.json`)

- JSON **array of strings** (each non-empty). At run start **`createInitialGameState`** picks one at random for **`GameState.organizationName`** (shown at the top of the controls panel). No ids or cross-references.

### Wanted levels (`content/wantedLevels.json`)

- **`WantedLevelTier`**: **`minHeat`** (inclusive floor on **`player.heat`**, 0–100), **`name`** (shown as **Wanted** in the HUD), **`maxAgents`** (cap on opposing agents placed on the map for that tier; **`executePlan`** may spawn up to this count when the tier index **increases** after resolve).
- Runtime **`GameState.wantedLevelTierIndex`** indexes **`catalog.wantedLevels`**; updated **once per **`executePlan`**** after all resolving missions have applied heat. **Never decreases** even if **`player.heat`** drops. When the computed index is **greater** than the value at resolve start, **`spawnOpposingAgentsAfterWantedEscalation`** runs (see **`agent.ts`**).

### Game balance (`content/balance.json`)

**`BalanceConfig`** (`types.ts`) — a **single object** slice of designer-tunable knobs. Every field has a Zod **default** equal to the legacy constant (**`DEFAULT_BALANCE`** in `types.ts`, the single source for schema defaults and code fallbacks), so `{}` is a valid file that changes nothing. **All numeric gameplay constants quoted elsewhere in this document are these defaults** — the balance file overrides them. Parsed as `catalog.balance`; edited in the editor's **Game Balance** view (each field has a plain-language tooltip + hint).

| Knob (default) | Applied where |
|---|---|
| `statusPositiveBonus` (10) / `statusNegativePenalty` (20) / `opposingAgentPenalty` (20) | `computeSuccessChanceBreakdown` via **`MissionSuccessOptions.balance`** (resolve + UI previews) |
| `dynamicTraitModifiers` (friend +5 / lover +10 / rival −5 / hatred −10 / hero +5 / wanted −5) | `dynamicTrait.ts` modifier functions (optional `modifiers` param; breakdown-from-roster reads `catalog.balance` itself) |
| `dynamicTraitRollPercent` (10) | `rollDynamicTraitsAfterMission` (passed by `executePlan`) |
| `infamySuccessDelta` (+5) / `infamyFailureDelta` (0) | baseline infamy in `executePlan` |
| `heatSuccessDelta` (0) / `heatFailureDelta` (+5) | baseline heat in `executePlan` (drives the wanted level) |
| `injuryChancePerAgentPercent` (20) | critical-failure injury rolls in `executePlan` (`min(100, per-agent × count)`) |
| `startingMaxCommandPoints` (5), `startingMaxRosterSize` (5), `startingMaxHireOffers` (3), `startingMaxConcurrentMissions` (2), `startingMaxParticipantsPerMission` (3) | `PlayerState` init in `createInitialGameState` (mission effects still mutate the live values) |
| `rerollHireOffersCp` (1) | `rerollHireOffers` + Main-Phase button |
| `eventMaxParticipants` (3) | event-mission crew cap in `assignMission` / `executePlan` / plan UI |
| `fireRehireCooldownTurns` (3) | `fireMinion` (now takes `catalog`) |
| `hireLevelInfamyThresholds` ([15, 35, 60, 85]) | infamy floors gating hire-pool `startingLevel` in `pickHireOfferTemplateIds` |
| `minionXpPerMission` (1) / `minionXpToLevel` (3) | `awardMissionResolutionExperience` (optional `xp` param, passed by `executePlan`) |
| `assetsPerLocationMin`/`Max` (1/3) | `initializeLocationAssetPlacements` at run start (every slot starts **hidden**) |
| `initialIntelSitesAtOne` (2) / `initialIntelSitesAtTwo` (1) | `rollInitialLocationIntelStates` at run start (opening site visibility — see **Site intel**) |
| `securityGainPerResolvedMission` (1) | `raiseSecurityAfterMissionAtLocation` |

Semantic check: `assetsPerLocationMin ≤ assetsPerLocationMax` (in **`collectContentIssues`**). The old exported constants (`OPPOSING_AGENT_SUCCESS_PENALTY`, `MINION_XP_*`, `REROLL_HIRE_OFFERS_CP`, `EVENT_MAX_PARTICIPANTS_PER_MISSION`, `MINION_FIRE_REHIRE_COOLDOWN_TURNS`, `DYNAMIC_TRAIT_ROLL_PERCENT`) remain as **deprecated aliases of the defaults** — new code must read `catalog.balance`.

## Runtime game state (`src/game/gameState.ts`)

Not persisted in JSON; created per session via **`createInitialGameState(catalog, rng?)`** (requires **`ContentCatalog`** to roll initial hire offers; pass a seeded **`Rng`** for deterministic runs — defaults to `Math.random`). **`executePlan(state, catalog, rng, newInstanceId?)`** likewise accepts an id generator (default `crypto.randomUUID`) so resolves are fully deterministic under a seeded RNG (tests, future replays). **Resolution is simultaneous by design**: every mission resolving in one `executePlan` reads site required/security traits from the **start-of-turn snapshot**, so an earlier mission's security bump that resolve never changes a later mission's requirements (pinned by `gameState.test.ts`).

- **`TurnPhase`**: `main` | `resolve` | `summary` — **`executePlan`** sets **`summary`** briefly; the game UI immediately calls **`advanceToNextTurn`**, so the player usually sees **`main`** again after **Execute Plan** with **`commandPoints`** refilled and **`turnNumber`** incremented (no separate **Next Turn** control).
- **`PlayerState`**: `commandPoints` / `maxCommandPoints` (new game: 5/5; **`advanceToNextTurn`** sets `commandPoints = maxCommandPoints + pendingBonusCommandPoints`, then clears **`pendingBonusCommandPoints`** to **0**), `infamy` (0–100, clamped; the score the player raises — mission success grants +5), `heat` (0–100, clamped; law-enforcement attention the player keeps down — mission failure grants +5; the **only** input to the wanted level), `minions` (`MinionInstance[]`), `assets` (`Record<assetId, quantity>`), **`maxRosterSize`** (default **5**; hire blocked at cap), **`maxHireOffers`** (default **3**; cap on random offers per resolve), **`maxConcurrentMissions`** (default **2**; assign blocked when active mission count is at cap; may exceed current active count after a decrease until missions finish), **`maxParticipantsPerMission`** (default **3**; assign UI and **`canAssignParticipants`** cap for **non-event** missions), **`pendingBonusCommandPoints`** (default **0**; one-shot CP added on top of **`maxCommandPoints`** at the next **`advanceToNextTurn`** after effects like **`grant_command_points_next_turn`**).
- **`GameState.wantedLevelTierIndex`**: index into **`catalog.wantedLevels`** (starts **0**). After **`executePlan`**, **`nextMonotonicWantedTierIndex`** sets **`max(previous index, tier implied by final `player.heat`)`** so escalation never rolls back when heat falls. If the new index is **greater** than the previous, opposing agents may spawn up to the new tier’s **`maxAgents`** (see **`spawnOpposingAgentsAfterWantedEscalation`**).
- **`GameState.organizationName`**: display string for the player’s evil organization, chosen once at **`createInitialGameState`** from **`catalog.organizationNames`**.
- **`GameState.activeOmegaPlanId`**: catalog **`OmegaPlanTemplate.id`** for this run, or **`null`** if **`catalog.omegaPlans`** is empty. Chosen once at **`createInitialGameState`** via **`pickRandomOmegaPlanId`**. Shown in the **Omega Plan** panel (three phases, three missions each, names from mission templates). Defines the active **`mapId`** for location scope. **`GameState.activeOmegaStageIndex`** and **`omegaStageProgress`** track which omega **row** must be cleared next and which slots of every row have succeeded; **`executePlan`** sets a slot flag on the succeeding mission's own stage row and advances the stage once the current row has **`requiredMissions`** successes (fewer than three when the designer authored it that way).
- **`GameState.currentEventTemplateId`**: **`EventTemplate.id`** for the **current rotating offer**, or **`null`** when **`catalog.events`** is empty. Initialized and re-rolled in **`executePlan`** via **`pickRandomEventTemplateId`**. **`GameState.eventOfferEngagedThisTurn`**: set **`true`** when an **event** mission is assigned in Main; cleared at **`executePlan`** / **`advanceToNextTurn`**. Drives whether **`expireEffects`** fire.
- **`GameState.activeSuccessModifiers`**: `{ delta: number; turnsRemaining: number }[]` — flat **success %** addends from **`add_success_chance_modifier`**; total summed into resolve + UI as **`eventSuccessModifierDelta`**. Each **`executePlan`** decrements **`turnsRemaining`** and drops finished entries **after** optional event expire effects apply.
- **`GameState.activeLairId`** / **`lairMissionIds`** / **`completedLairUpgradeMissionIds`**: random lair at run start when **`catalog.lairs`** is non-empty; **`lairMissionIds`** is the pool for the Lair **Missions** tab (starts as **`availableMissionIds`**). **`completedLairUpgradeMissionIds`** lists upgrade mission template ids that finished **successfully** this run. Assigning from the lair allows **`missionTemplateId`** in **`lairMissionIds`** **or** in **`pendingLairUpgradeMissionIds`** ( **`upgradeMissionIds`** minus **`completedLairUpgradeMissionIds`**).
- **`GameState.locationSecurityStates`**: one **`LocationSecurityState`** per **playable** location this run, initialized at security **`0`**. Shown in the **Locations** panel; **increments on mission resolution** when the resolved mission’s target maps to that location, capped by **`locationLevel`** (see **Locations**).
- **`GameState.locationIntelStates`**: one **`LocationIntelState`** per **playable** location, seeded at run start by **`rollInitialLocationIntelStates`** (**`initialIntelSitesAtOne`** sites at intel 1 + **`initialIntelSitesAtTwo`** more at intel 2, distinct picks; the rest at **`0`**). Gates the player-facing view of that site's assets and opposing agents; moved by the **`intel_level_delta*`** effects during `applyMissionEffects` and threaded back out of **`executePlan`** like the security states. See **Site intel**.
- **`GameState.locationRequiredTraits`** / **`locationSecurityTraits`**: `Record<locationId, string[]>` — rolled in **`createInitialGameState`** for playable locations only; merged into **`missionSuccessOptionsForTarget`** for mission success and UI when the target has a location id (**`revealedSecurityTraitIds`** applies the security stack slice).
- **`GameState.locationAssetSlots`**: **`LocationAssetPlacement[]`** (each **`locationId`** + **`slots`** of **`LocationAssetSlot`**). Initialized in **`createInitialGameState`** only for playable locations: each gets **1–3** distinct random **`catalog.assets`** ids (none if the asset catalog is empty); all slots start **occupied** and **hidden**, and nothing is revealed up front — opening visibility comes from seeded **intel** (see **Site intel**). The Locations panel shows label **Asset** with value **"Asset"** when the player knows only that the slot exists, catalog **name** once its contents are known, or **"—"** when **`empty`** — and **omits the row entirely** when site intel is 0 and the slot has not been revealed another way (see **Site intel**); in **Main Phase**, listed **occupied** rows are **draggable chips** for targeting, carrying their **effective** visibility (`main.ts`).
- **`GameState.opposingAgentInstances`** / **`locationAgentPresence`**: **`AgentInstance[]`** for all spawned opposing agents this run, plus one **`LocationAgentPresence`** row per playable location (**`agentInstanceIds`** empty at start). Each **`AgentInstance`** includes **`catalogVisibility`** (`hidden` \| `revealed`; spawns default **hidden**). **`spawnOpposingAgentsAfterWantedEscalation`** (on wanted tier index increase) appends instances and presence entries; at most one opposing instance per **agent template** id at a time. Designers do not author placement in JSON. **`executePlan`** returns updated **`opposingAgentInstances`** every resolve (reveals at mission sites + tier spawns merge into the same locals).
- **Critical failure (injury)**: If a mission **fails** with **`getMissionTargetLocationId(am.target)`** set and **≥1** opposing agent at that location (count **`all`**, same as the success-roll penalty), **`onFailureEffects`** apply first via **`applyMissionEffects`**, then each **participant** rolls once for the **`injured`** trait with independent chance **`min(100, 20 × agentCount)`**% (**`applyCriticalFailureInjuryRolls`** in **`missionEffects.ts`**; no duplicate trait ids). Targets with **no** mission location id (**`none`** / **`minion`**) never trigger this. **`mission_completed`** logs **`criticalFailure`**, agent count, injury **%**, and which participants gained **`injured`** from that roll pass for the activity panel.
- **`GameState.availableMinionTemplateIds`**: template ids currently offered for hire, drawn by **`pickHireOfferTemplateIds`** (`gameState.ts`). Every draw excludes templates **already on the roster** and applies the **infamy level gate** (see below), then shuffles and cuts to **`maxHireOffers`**. Drawn at game start, at the end of each **`executePlan`** (using **post-resolve** infamy, so a mission that crosses a threshold widens that same draw), and on demand during **Main Phase** for **1 CP** (**`rerollHireOffers`**). Hiring removes that template id from the list for the rest of the phase until the next reroll. **`hireMinion`** only accepts ids in this list.
- **Hire-pool infamy gate**: **`balance.hireLevelInfamyThresholds`** (default **`[15, 35, 60, 85]`**) is an ascending list of infamy floors; **`maxHireableStartingLevel(infamy, thresholds)`** returns **`1 + (thresholds reached)`**, and only templates whose **`startingLevel`** is at or under that cap are drawn. Level 1 is always on offer, so rising **infamy** — the stat mission success builds — is what brings better recruits to the door. If the cap leaves **no** unhired template (every low-level one is already on the roster), the draw falls back to the **lowest `startingLevel` still unhired** rather than offering nothing. **`collectContentIssues`** rejects non-ascending thresholds and flags any minion whose **`startingLevel`** exceeds **`1 + thresholds.length`** as unreachable.
- **`ActiveMission`**: `id`, `missionTemplateId`, **`target`** (**`MissionTarget`**), **`missionSource`** (`lair` | `omega` | `event`), **`omegaStageIndex`** / **`omegaSlotIndex`** (set when source is omega; **`null`** for lair and event), `participantInstanceIds` (1–**`maxParticipantsPerMission`** for lair/omega; **1–3** for event), **`plannedAssetIds`** (aligned with template **`requiredAssetIds`** — `null` = empty slot at assign; non-null entries were deducted from **`player.assets`** at assign), `turnsRemaining` (starts at `MissionTemplate.durationTurns`), **`startedOnTurn`**. Missions tick **only** when the player executes the plan: each active mission decrements `turnsRemaining`; at **0**, success is rolled vs **`successChancePercent`** with **`missionSuccessOptionsForTarget(state, target)`**, **`assignedAssetIds: am.plannedAssetIds`** when the template lists required assets (otherwise **`playerAssets: player.assets`** for asset-only missions without slots), **`dynamicTraitDelta`** from **`dynamicTraitSuccessModifierFromFullRoster`**, **`eventSuccessModifierDelta`** from **`activeSuccessModifiers`**, **`opposingAgentPenaltyCount`** from **all** agents at **`getMissionTargetLocationId(target)`** when set, plus **`traitsCatalog: catalog.traits`** (`mission.ts` + **`executePlan`** in **`gameState.ts`**); baseline infamy (+5 success / 0 failure) and baseline heat (0 success / +5 failure) are applied, then **`onSuccessEffects`** or **`onFailureEffects`** from the template via **`applyMissionEffects`** (**`missionEffects.ts`**), then **`rollDynamicTraitsAfterMission`** updates **`dynamicTraits`** and supplies optional **`mission_completed.dynamicTraitChanges`**. On **success**, **`executePlan`** also applies **`unlock_lair_mission`** entries (append to **`lairMissionIds`**) and, if the resolved mission id is in the active lair’s **`upgradeMissionIds`**, appends it to **`completedLairUpgradeMissionIds`**. **`mission_completed`** logs **`infamyDelta`** / **`heatDelta`** = total change for each (baseline + effect **`infamy_delta`** / **`heat_delta`** entries, clamped) plus **`baselineInfamyDelta`** / **`baselineHeatDelta`** and **`templateEffectDescriptions`** for the activity feed. The same tick applies **participant XP** (and possible **level-ups** + **`minion_leveled_up`** events), **+1 security** at the resolved target location when **`getMissionTargetLocationId(am.target)`** is set (per-location cap), **reveal all opposing agents at that location** when the target maps to a site and at least one agent is present, **`asset_gained`** when a steal effect fires, and **`mission_completed`** logging.
- **`GameState.activityLog`**: **`TurnActivityEntry[]`** (each **`turnNumber`** + **`events`**). Main-phase actions append via **`appendActivityEvent`**; **`executePlan`** batches resolve outcomes with **`mergeResolveActivityEventsIntoActivityLog`**: **`mission_completed`** rows (with **`target: MissionTarget`**, optional **critical failure** injury fields, optional **`dynamicTraitChanges`**) plus any **`minion_leveled_up`** rows (and other **`ActivityEvent`** kinds as features grow). New turn buckets are **prepended** so the **Activity** panel shows **newest turn first**; the log is **not** cleared when the turn advances (session-only persistence until save/load exists). **`ActivityEvent`** union includes at least: `mission_completed`, `mission_started`, `mission_cancelled` (each carries **`target`**), **`mission_aborted`** (`activeMissionId`, `missionTemplateId`, `target`, `reason: "missing_template" | "invalid_participants"` — a resolving mission whose template or roster is invalid is **not silently dropped**: committed `plannedAssetIds` are refunded with `asset_gained` rows and this event is logged; should be unreachable today, exists so future systems fail loudly), `minion_hired`, `minion_rehired`, `minion_fired`, `minion_leveled_up` (`instanceId`, `templateId`, `newLevel`, optional `traitId`), `asset_gained`, `asset_lost`, **`event_rotated_in`**, **`event_expired`**.

Assignment rules: enforced in **`assignMission`** — **`missionTemplate.targetType`** must match the provided **`MissionTarget`**; **playable** location scope for **location** / **asset** targets (**`activeLocationIds`**); **`missionSource`** **`lair`**: mission id must be in **`GameState.lairMissionIds`** **or** in **`pendingLairUpgradeMissionIds`** for the active lair; omega indices null; **`omega`**: active plan, **`activeOmegaStageIndex`**, and mission id must match **`omegaSlotMissionId`** for the chosen slot; **`event`**: **`missionTemplateId`** must equal **`currentEventTemplateId`**, omega indices null, **1–3** participants; sufficient **`startCommandPoints`**; participants not already on a mission; target minion not in participant list; **`activeMissions.length < maxConcurrentMissions`**; **`plannedAssetIds`** must match **`requiredAssetIds`** length, each non-null entry must equal that slot’s required id, and per-id tallies must not exceed **`player.assets`** (deducted on assign, logged as **`asset_lost`**; **`cancelMission`** refunds with **`asset_gained`**).

## UI / navigation

- Screens: Main (Play, Settings), Settings (Back with return stack from pause vs main), Game (canvas + **game UI layer**: **controls + Activity** (stacked) / **Omega plan + Locations** / **Minions + Assets**; **bottom HUD** for **Execute Plan** (resolves and advances the turn), turn/phase, and Pause), Pause overlay (Back / Quit / Settings).
- **Game panel** (`index.html`): **organization name** at the top (from **`GameState.organizationName`**), then stats (CP, infamy, heat, active missions count as **`current / maxConcurrentMissions` missions** in the UI summary), **Plan mission**: **Mission** slot (drag from Omega / Lair / **Events** tab offer) and **Target** slot (drag **location** card, **asset** chip on a location, or **minion** from roster — JSON drag payloads); dynamic **target** label and **hidden** target field when **`targetType === "none"`**; participant slots (**count** = **`max(maxParticipantsPerMission, 3)`** when an **event** is staged so three drops are available, else **`maxParticipantsPerMission`**, up to UI capacity 12)—drag from **roster**, **×** to clear; **Required Assets** slots when the mission template lists **`requiredAssetIds`** (drag matching **Assets** panel cards); **`assignMission`** receives **`MissionTarget`** and **`plannedAssetIds`**. **Activity panel**: includes **`mission_completed`** / **`mission_started`** / **`mission_cancelled`** text derived from **`MissionTarget`**; **`mission_completed`** includes baseline infamy/heat and template effect summaries (and **roll vs true success %**, including hidden opposing agents and **dynamic trait** / **event** modifiers when applicable), optional sentences built from **`dynamicTraitChanges`**, plus **critical failure** injury summary when a failed location-backed mission had opposing agents; **`event_rotated_in`** / **`event_expired`** lines for the global event cycle. **Omega Plan panel**: the plan header's stage hint reads **`Active phase: N · Missions complete: X of Y`** for the current phase, and each phase section header shows a **Complete X of Y** line (X = that stage's **`requiredMissions`**, Y = its three mission slots); the phase progress bar and the status-bar Omega meter are scaled to the required counts, not to a fixed 9, and a slot left undone in a cleared phase reads **Skipped** instead of Complete. **Omega column**: **Locations** can be filtered by **Economic / Political / Military** tabs (sites sorted by **`locationLevel`** ascending, then name within each tab); each **location card** shows an **Intel level** row (`n / 3`, tooltip listing what each level unlocks) and lists **Agents** at that site when any opposing agent is **visible to the player** (portrait chips — `resolveAgentCardArt`, minion placeholder fallback); agents still hidden from the player are left out of the card entirely. **Lair** panel: **Missions** vs **Upgrades** tabs — **Missions** lists **`lairMissionIds`**; **Upgrades** lists pending **`upgradeMissionIds`** (excluding **`completedLairUpgradeMissionIds`**); draggable mission cards use **`missionSource === "lair"`** from either tab. **Missions** panel (renamed from a single Active Mission column): **Active Missions** vs **Events** tabs — **Active Missions** lists all **`activeMissions`** with **Source** including **Event** for **`missionSource === "event"`**; **Events** shows only the draggable **current offer** (in-progress event missions appear only under **Active Missions**). Minion roster / hire cards and assign chips show **dynamic trait** pills in the same **Traits** row as catalog traits. The **Available to hire** section carries a **recruit-level line** under its heading (`Recruiting up to level N. Level N+1 recruits appear at X infamy (now Y).`, or `— every tier unlocked` at the top tier) so the infamy gate on the pool is visible rather than silent.
- `navigation.ts` wires visibility and **starts/stops** the canvas RAF loop when the game screen is visible and not paused.
- Canvas draws a placeholder “Game” label; rules and economy are driven from the DOM panel, not the canvas.

## Content editor (dev-only)

Open **`http://localhost:5173/editor.html`** while `npm run dev` is running. Vanilla-TS app under **`src/editor/`**; it is **never bundled into production builds** (Vite's build input is `index.html` only, and the API plugin is `apply: "serve"`).

- **Dev API** (plugin in `vite.config.ts`): `GET /__content-api/slices` reads every `CONTENT_MANIFEST` file; `PUT /__content-api/slices` re-runs **`parseContentCatalog`** on the whole draft and **refuses to write when issues exist** (422 with the `ContentIssue[]`), so files on disk always pass the build gate. Writes go through **`serializeContentSlice`** and only touch changed slices. **The first save canonicalizes formatting of every content file once**; after that, diffs are minimal. Saving triggers HMR on the game page (live preview loop). **Art uploads**: `GET /__content-api/art` lists `public/assets/cards/custom/*.webp` (name, path, animated flag); `POST /__content-api/art?name=<slug>[&overwrite=1]` takes raw WebP bytes — filename is slugged, magic bytes verified (a renamed PNG is rejected), ≤ 8 MB, existing file → 409 unless overwriting. Shared pure helpers (slug + `RIFF…WEBP` + `ANIM`-chunk detection) live in **`src/editor/artFiles.ts`** (browser-safe; used by both the plugin and the editor page; unit-tested). Uploads live under `public/`, so Vite serves them in dev, copies them into `dist/` on build, and they are committed to git beside the content that references them.
- **Draft model** (`src/editor/store.ts`): in-memory copy of all raw slices; every edit snapshots (undo, depth 50), re-validates with the real schema pipeline, and mirrors to `localStorage` (restore prompt after an accidental tab close). Invalid work-in-progress lives in the draft, never on disk.
- **Layout** (`src/editor/main.ts`): slice sidebar (issue-count badges) → entity list (`+ New`, per-entity issue dots) → detail form → global issues panel (click an issue to jump to the entity). Header: **Save** (disabled while issues exist), **Undo**, **Unreferenced…** report, status.
- **Forms** (`src/editor/forms/`): hand-written per slice. Every cross-reference field is an **id picker** over the current draft. The **Game Balance** view (`balanceForm.ts`) is a singleton form (no entity list / New / Duplicate / Delete / Rename): grouped fieldsets for every scalar `BalanceConfig` knob, each with a **plain-language tooltip + hint** explaining its gameplay effect, plus a live **formula strip** showing the success-chance formula with the draft's current numbers. Two knobs are **not** scalars and get hand-rolled blocks: **`dynamicTraitModifiers`** (nested object, one row per bond kind) and **`hireLevelInfamyThresholds`** (ascending list — a `listEditor` of `Level N at infamy …` rows with add/reorder/remove, plus a summary strip of the resulting infamy → level ladder). Missions/events share one form with the **effects list editor** (kind-specific fields per `MissionEffect`; kinds incompatible with the current `targetType` are disabled via **`effectKindTargetTypeRequirement`**; a note shows the `orderedMissionEffects` resolution order) and a **success-chance preview** (pick up to 3 minion templates; real `computeSuccessChanceBreakdown`). Omega plans render the literal 3×3 mission grid, each stage row carrying a **must complete N of 3** number input for that phase's **`requiredMissions`**. Minions/agents include a `startingDynamicTraits` sub-editor. Inputs commit on `change` (blur/enter), not per keystroke. Every art-path field (`cardArt` on minions, agents, missions, events, locations, assets, lairs, omega plans; `profilePic` on player profiles) uses the **art field widget** (`src/editor/artField.ts`): live thumbnail preview with an **animated ✓ / static webp / file not found** badge, editable path input, **Upload .webp…** (client-side preflight, name prompt seeded `<slice>-<id>`, overwrite confirm on 409), and a picker over previously uploaded files. **Traits, wanted levels, maps, and organization names intentionally have no art fields.**
- **Tools**: **Rename id…** (uses `applyIdRenameRaw`, updates all referents), **Duplicate** (`-copy` id suffix), **Delete…** with reference-impact modal (`referencesTo` over the draft), **Unreferenced…** report (`unreferencedIds`; caveat shown that unreferenced ≠ unused — some content is only rolled/offered at runtime).
- Entity **id fields are read-only** in forms — ids change only through Rename so references can never silently dangle. New entities prompt for their id.

## Conventions

- **Adding a catalog slice**: add the key to **`CONTENT_SLICE_KEYS`** (drives **`CONTENT_MANIFEST`**, `RawContentSlices`, and the validator CLI), an entity schema + entry in **`contentSliceSchemas`**, a normalizer in **`parseContentSlices`**, semantic checks in **`collectContentIssues`**, the field on **`ContentCatalog`**, the static import in `loadContent.ts`, and (if it holds id references) the walkers in `contentRefs.ts`.
- Report content problems as **`ContentIssue`** rows (accumulate; never throw on the first problem inside the schema layer). Only `parseCatalog` throws, and it throws everything at once.
- Any tool that **writes** `content/*.json` must serialize through **`serializeContentSlice`** so diffs stay canonical.
- **Card art paths** (`cardArt` / `profilePic`) are site-root paths to files under `public/` (uploads go to `public/assets/cards/custom/`, WebP only — animated WebP plays natively in the plain `<img>` from `createCardArtImg`, no code needed). `scripts/validate-content.ts` fails the build when a referenced art file does not exist on disk (Node-only check; the browser schema validates the string shape only).
- Keep **designer data** in `content/*.json` and **runtime/progression** in **`gameState.ts`** and related TS modules (`types.ts`, `minion.ts`, `agent.ts`, `wantedLevel.ts`, `mission.ts`).
- After editing JSON, run **`npm run content:validate`** (or `npm run build`) before assuming content is valid. After editing rules-engine code, run **`npm test`** and **`npm run typecheck`** (the build runs both validation and typecheck; Vite alone does not typecheck).
- Rules-engine functions take an injected **`Rng`** (and an id generator where they mint instances) — never call `Math.random` / `crypto.randomUUID` directly inside `src/game/`.
- **New gameplay constants go in `BalanceConfig`**, not as code literals: add the field + default to `DEFAULT_BALANCE` (`types.ts`), the schema field in `balanceConfigSchema`, thread it from `catalog.balance`, and add the row (label + plain-language tooltip) to the editor's `balanceForm.ts`. A **non-scalar** knob (array or nested object) must also be excluded from that file's **`ScalarKey`** union and rendered by its own block — the generic `numberInput` loop only handles numbers.
- UI event handlers in `main.ts` route state transitions through **`dispatch`** rather than assigning `state` themselves.

## Not implemented yet (non-exhaustive)

- Save/load persistence (note: `GameState` is plain JSON-serializable data; when implemented, add a Zod schema + `saveVersion`).
- Content editor niceties: field-anchored inline errors (currently entity-level), undo persistence across sessions.
- **Moving** opposing agents between locations (spawn on wanted escalation only today).
- Omega plan activation/progress/win (beyond per-slot success, **`requiredMissions`** phase clears, and stage index in **`executePlan`**).
- Rich gameplay on canvas beyond placeholder label.
